---
title: "Get API description"
sidebarTitle: "API description"
description: "Unauthenticated endpoint returning the API description and a link to these docs."
icon: "circle-info"
---

<Info>
  **`GET /api/v1`** · Unauthenticated
</Info>

Returns the API description and a link to these docs. Useful as a health/version check.

## Request

```bash
curl https://www.oceanx.trade/api/v1
```

No authentication is required for this endpoint.